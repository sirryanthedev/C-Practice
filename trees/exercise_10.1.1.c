#include <stdio.h>
#include <stdlib.h>

struct node_bt{
    int data; // data to store
    struct node_bt* left; // left child
    struct node_bt* right; // right child
};

int main(){
    return 0;
}